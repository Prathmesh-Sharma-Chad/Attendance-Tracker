#pragma once
#include <iostream>
#include <iomanip>
#include <cctype>
#include <fstream>
#include <vector>
#include <algorithm>

std::vector<std::string> Subjects; // subjects name
std::vector<int> total_attendance; // classes attended
std::vector<int> classes_held; // classes held so far

